get_version(3)          Library Functions Manual         get_version(3)

NAME
       get_version - Get the version.

LIBRARY
       codata (libcodata, -lcodata)

SYNOPSIS
         function get_version()result(fptr)

DESCRIPTION
       Get the version of library.

RETURN VALUE
       o character(len=:), pointer :: fptr
              Version as a deferred-length pointer.

EXAMPLE
       Minimal example

               print *, get_version()

codata                      23 December 2025             get_version(3)
