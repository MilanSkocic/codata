$DEFINE CURRENT=${YEAR}
!=======================================================================
! MODULE: CODATA_CONSTANTS_${YEAR}
!=======================================================================
!{{{1
module codata__constants_${YEAR}
!! Codata Constants - Autogenerated
use, intrinsic :: iso_c_binding, only: c_null_char, c_int
use stdlib_kinds, only: dp, int32
use codata__constants_type
implicit none(type,external)
private

$IF CURRENT==2022
integer(int32), parameter, public :: YEAR = ${YEAR} !! Year of release.
integer(c_int), protected, public, bind(C, name="YEAR") :: capi_YEAR = YEAR !capi
$ELSE
integer(int32), parameter, public :: YEAR_${YEAR} = ${YEAR} !! Year of release.
integer(c_int), protected, public, bind(C, name="YEAR_${YEAR}") :: capi_YEAR = YEAR_${YEAR} !capi
$ENDIF

$INCLUDE ../data/build/codata_constants_${YEAR}.f90

end module codata__constants_${YEAR}
